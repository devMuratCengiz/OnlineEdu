@model List<ResultContactDto>
@{
    ViewData["Title"] = "Contact";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}


<table class="table table-hover">
    <tr style="font-weight:bold;">
        <th>#</th>
        <th>Address</th>
        <th>Phone</th>
        <th>Mail</th>
        <th>Map URL</th>
    </tr>

    @foreach (var item in Model)
    {
        count++;
        <tr>
            <th>@count</th>
            <td>@item.Address</td>
            <td>@item.Phone</td>
            <td>@item.Email</td>
            <td><a href="@item.Map" target="_blank"></a></td>
            <td>
                <button onclick="deleteItem(@item.Id)" class="btn btn-outline-danger">Delete</button>
                <a asp-action="Update" asp-controller="Contact" asp-area="Admin" asp-route-id="@item.Id" class="btn btn-outline-info">Update</a>
            </td>
        </tr>
    }
</table>
<br />
<a asp-action="Create" asp-controller="Contact" asp-area="Admin" class="btn btn-outline-primary">Add</a>

<script>

    function deleteItem(id){
        Swal.fire({
      title: "Are you sure?",
      text: "You won't be able to revert this!",
      icon: "warning",
      showCancelButton: true,
      confirmButtonColor: "#3085d6",
      cancelButtonColor: "#d33",
      confirmButtonText: "Yes, delete it!",
    }).then((result) => {

        var controllerName = "@ViewData["Title"]"
        window.location.href = "/Admin/"+controllerName+"/Delete/" + id;

    });
    }
</script>